// MUI Imports
import type { Theme } from '@mui/material/styles'

const avatar: Theme['components'] = {
  MuiAvatarGroup: {
    styleOverrides: {
      root: ({ theme }) => ({})
    }
  },
  MuiAvatar: {
    styleOverrides: {
      root: ({ theme }) => ({})
    }
  }
}

export default avatar
